apiVersion: v1
kind: Service
metadata:
  name: grocery-genie-service
  namespace: staging
  labels:
    app: grocery-genie
    environment: staging
spec:
  selector:
    app: grocery-genie
    environment: staging
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
